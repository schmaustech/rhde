---
 - name: "Playbook to test the inventory_hostname and ansible_hostname"
   hosts: edge_manager
   tasks:
     - name: Get hostname
       shell: echo $HOSTNAME
       register: result

    - name: Print the var
      debug:
        var: result
